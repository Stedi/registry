{
  "type": "object",
  "properties": {
    "options": {
      "type": "array",
      "description": "These are the processing options.",
      "example": ["PRODUCE_PAPERLESS_SHIPPING_FORMAT", "SUPPRESS_ACCESS_EMAILS"],
      "items": {
        "type": "string",
        "enum": ["PRODUCE_PAPERLESS_SHIPPING_FORMAT", "SUPPRESS_ADDITIONAL_LANGUAGES", "SUPPRESS_ACCESS_EMAILS"]
      }
    }
  },
  "description": "These are to indicate how the email notifications for the pending shipment to be processed.",
  "default": {
    "options": ["PRODUCE_PAPERLESS_SHIPPING_FORMAT", "SUPPRESS_ACCESS_EMAILS"]
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
