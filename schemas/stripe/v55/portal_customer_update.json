{
  "name": "portal_customer_update",
  "schema": {
    "description": "",
    "properties": {
      "allowed_updates": {
        "enum": [
          "address",
          "email",
          "phone",
          "shipping",
          "tax_id"
        ],
        "type": "string"
      },
      "enabled": {
        "description": "Whether the feature is enabled.",
        "type": "boolean"
      }
    },
    "required": [
      "allowed_updates",
      "enabled"
    ],
    "title": "PortalCustomerUpdate",
    "type": "object",
    "x-expandableFields": [],
    "default": {
      "allowed_updates": "address",
      "enabled": true
    }
  }
}