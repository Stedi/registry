{
  "name": "networks",
  "schema": {
    "description": "",
    "properties": {
      "available": {
        "maxLength": 5000,
        "type": "string"
      },
      "preferred": {
        "description": "The preferred network for the card.",
        "maxLength": 5000,
        "nullable": true,
        "type": "string"
      }
    },
    "required": [
      "available"
    ],
    "title": "networks",
    "type": "object",
    "x-expandableFields": [],
    "default": {
      "available": "string",
      "preferred": "string"
    }
  }
}