{
  "description": "",
  "properties": {
    "available": {
      "maxLength": 5000,
      "type": "string"
    },
    "preferred": {
      "description": "The preferred network for the card.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "available"
  ],
  "title": "networks",
  "type": "object",
  "default": {
    "available": "string",
    "preferred": "string"
  }
}