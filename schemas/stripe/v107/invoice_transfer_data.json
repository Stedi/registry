{
  "name": "invoice_transfer_data",
  "schema": {
    "description": "",
    "properties": {
      "amount": {
        "description": "The amount in %s that will be transferred to the destination account when the invoice is paid. By default, the entire amount is transferred to the destination.",
        "nullable": true,
        "type": "integer"
      },
      "destination": {
        "maxLength": 5000,
        "type": "string"
      }
    },
    "required": [
      "destination"
    ],
    "title": "InvoiceTransferData",
    "type": "object",
    "x-expandableFields": [
      "destination"
    ],
    "default": {
      "amount": 0,
      "destination": "string"
    }
  }
}